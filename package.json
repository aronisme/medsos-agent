{
  "name": "fb-ig-poster",
  "version": "1.0.0",
  "private": true,
  "description": "FB & IG Auto Poster - schedule & post content to Facebook Pages and Instagram Business Accounts",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c magenta,cyan \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
